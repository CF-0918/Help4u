import 'package:flutter/material.dart';

class buildCancelledTab extends StatefulWidget {
  const buildCancelledTab({super.key});

  @override
  State<buildCancelledTab> createState() => _buildCancelledTabState();
}

class _buildCancelledTabState extends State<buildCancelledTab> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
